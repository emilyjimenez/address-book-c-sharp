@{
  Layout = "_Layout";
}
<h1>Contacts</h1>
@if (@Model.Count == 0)
{
  <h3>You have not added any contacts yet!</h3>
}
@foreach (var name in Model)
{
  <h4><a href='/names/@name.GetId()'>@name.GetName()</a></h4>
}
<p><a href='/'>Back to main page</a></p>
